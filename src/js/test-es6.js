const arrowFunc = () => {
    console.log("ES6 func is loaded");
    const doMath = window.innerWidth - window.innerHeight;
    console.log("window.innerWidth - window.innerHeight", doMath);
};

export default arrowFunc;
